.message-date__log
  .user
    = message.user.name
  .date
    = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
.message-date__comment
  - if message.content.present?
    %p.message-date__comment--contence
      = message.content
  = image_tag message.image.url, class: 'image' if message.image.present?
  